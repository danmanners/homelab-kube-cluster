--- # ArgoCD Certs
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-homelab-cert
spec:
  commonName: argocd.homelab.danmanners.com
  secretName: argocd-homelab-cert
  dnsNames:
    - argocd.homelab.danmanners.com
    - argocd.danmanners.com
  issuerRef:
    name: acme-prod
    kind: ClusterIssuer
