apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-cert
spec:
  commonName: traefik.homelab.danmanners.com
  secretName: traefik-cert
  dnsNames:
  - traefik.homelab.danmanners.com
  issuerRef:
    name: acme-prod
    kind: ClusterIssuer
