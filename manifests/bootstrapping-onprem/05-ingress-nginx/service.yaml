apiVersion: v1
kind: Service
metadata:
  name: nginx-bgp-ip
  annotations:
    external-dns.alpha.kubernetes.io/hostname: nginx.homelab.danmanners.com
spec:
  type: ExternalName
  externalName: 64.99.148.231 # External IP
  # externalName: 10.4.240.2 # Internal IP
