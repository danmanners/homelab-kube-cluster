apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-cert
spec:
  commonName: argocd.cloud.danmanners.com
  secretName: argocd-cert
  dnsNames:
  - argocd.cloud.danmanners.com
  issuerRef:
    name: acme-prod
    kind: ClusterIssuer
