# How to use these files

You can rename this directory from `examples` to whatever you want to name your cluster.

1. Make sure to update the `hosts.ini` file with all the appropriate host information.
